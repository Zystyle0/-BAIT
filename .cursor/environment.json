{
  "name": "Calorie Bank",
  "install": "python3 -m venv .venv && . .venv/bin/activate && pip install --upgrade pip && pip install -r requirements-dev.txt",
  "terminals": [
    {
      "name": "api",
      "command": ". .venv/bin/activate && uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload"
    }
  ],
  "ports": [
    {
      "name": "Calorie Bank web",
      "port": 8000
    }
  ]
}
