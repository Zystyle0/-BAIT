{
  "name": "Calorie Bank",
  "install": "sudo apt-get update && sudo apt-get install -y python3.12-venv && python3 -m venv .venv && . .venv/bin/activate && pip install --upgrade pip && pip install -r requirements-dev.txt",
  "terminals": [
    {
      "name": "api",
      "command": ". .venv/bin/activate && uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload",
      "description": "Calorie Bank FastAPI server on http://localhost:8000 (auto-reloads on code changes)."
    }
  ],
  "ports": [
    {
      "name": "Calorie Bank web",
      "port": 8000
    }
  ]
}
